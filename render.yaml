services:
  - type: web
    name: easy-video-service
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "5000"
      - key: PYTHON_ENV
        value: production
    autoDeploy: true
